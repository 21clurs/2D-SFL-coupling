#include "solidmesh.h"

bool SolidMesh::checkCollisionAndSnap(Eigen::Vector2d& x){
    // iterate through faces
    for (size_t i =0; i<faces.size(); i++){
        // check if point is on consistent side of all faces (i.e. "inside")
        

    }
}